/**
 * Provides classes for working with flow summaries in PHP.
 */

private import codeql.php.DataFlow

/**
 * A callable with a flow summary, providing a specification of data flow
 * through the callable.
 */
module FlowSummary {
  // Placeholder: flow summary infrastructure
}
