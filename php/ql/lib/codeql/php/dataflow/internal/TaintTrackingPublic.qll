/**
 * Provides public taint-tracking predicates for PHP.
 */

/**
 * Holds if taint propagates from `source` to `sink` in zero or more steps.
 */
module Public {
  // Placeholder: taint tracking public API
}
